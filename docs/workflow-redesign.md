# Guided workflow release — 2026.09.26.01

The approved static preview in `docs/preview` is integrated with the production PHP views and existing JavaScript controllers. The release keeps the real endpoint validation, configuration locks/revisions, snapshot identities, source-retention approvals, schedule timing, and execution behavior.

Automatic snapshots and Backup copies use focused in-page setup, then compact section editing. Shared backup settings retain a separate save boundary; SSH connection setup returns to the job draft. Snapshot selection/search share a header, optional filters remain available, and browsing height is bounded. Existing Tools and Automation links resolve to the new task navigation. Migration uses the existing preview and status results for Select, Review, and Run.

Verification covers the full disposable reliability suite; scoped-save isolation and runtime failure; source-retention binding and expiry; scheduling/DST and retention regressions; production workspace/browser journeys; 10,000-snapshot captured selection; recovery logs and review; 1366×768 guided-step bounds; narrow layouts and light/dark screenshots; PHP/JavaScript syntax; and release package verification. The older `config_browser.cjs` predates the prior release's editor and refers to removed controls; its relevant journeys are covered by `workspace_browser.cjs`, `source_retention_browser.cjs`, and `guided_workflow_browser.cjs`. No backend execution changes are introduced.

The working preview remains a simulated design reference. Production tests render the actual PHP pages and route browser requests to controlled fixtures; they do not execute operations on user pools.
